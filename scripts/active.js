window.addEventListener("load",function(){const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.intersectionRatio>0){entry.target.classList.add("active");}else{entry.target.classList.remove("active");}});});const first_characters=document.querySelectorAll("p");first_characters.forEach((el)=>{const text=el.innerHTML.trim();if(text.length>0)el.innerHTML=`<span class="first-letter">${text[0]}</span>`+text.slice(1);observer.observe(el);});const virie_header=document.querySelector("#virie-header");observer.observe(virie_header);});